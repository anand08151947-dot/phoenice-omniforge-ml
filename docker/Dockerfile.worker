FROM python:3.12-slim
WORKDIR /app
COPY pyproject.toml .
COPY src/ src/
RUN pip install uv && uv pip install --system -e ".[dev]"
CMD ["celery", "-A", "omniforge.tasks.celery_app", "worker", "--loglevel=info", "--concurrency=2"]
